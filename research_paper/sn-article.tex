%Version 3.1 December 2024
% See section 11 of the User Manual for version history
%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%                                                                 %%
%% Please do not use \input{...} to include other tex files.       %%
%% Submit your LaTeX manuscript as one .tex document.              %%
%%                                                                 %%
%% All additional figures and files should be attached             %%
%% separately and not embedded in the \TeX\ document itself.       %%
%%                                                                 %%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

%%\documentclass[referee,sn-basic]{sn-jnl}% referee option is meant for double line spacing

%%=======================================================%%
%% to print line numbers in the margin use lineno option %%
%%=======================================================%%

%%\documentclass[lineno,pdflatex,sn-basic]{sn-jnl}% Basic Springer Nature Reference Style/Chemistry Reference Style

%%=========================================================================================%%
%% the documentclass is set to pdflatex as default. You can delete it if not appropriate.  %%
%%=========================================================================================%%

%%\documentclass[sn-basic]{sn-jnl}% Basic Springer Nature Reference Style/Chemistry Reference Style

%%Note: the following reference styles support Namedate and Numbered referencing. By default the style follows the most common style. To switch between the options you can add or remove Numbered in the optional parenthesis. 
%%The option is available for: sn-basic.bst, sn-chicago.bst%  
 
%%\documentclass[pdflatex,sn-nature]{sn-jnl}% Style for submissions to Nature Portfolio journals
%%\documentclass[pdflatex,sn-basic]{sn-jnl}% Basic Springer Nature Reference Style/Chemistry Reference Style
\documentclass[pdflatex,sn-mathphys-num]{sn-jnl}% Math and Physical Sciences Numbered Reference Style
%%\documentclass[pdflatex,sn-mathphys-ay]{sn-jnl}% Math and Physical Sciences Author Year Reference Style
%%\documentclass[pdflatex,sn-aps]{sn-jnl}% American Physical Society (APS) Reference Style
%%\documentclass[pdflatex,sn-vancouver-num]{sn-jnl}% Vancouver Numbered Reference Style
%%\documentclass[pdflatex,sn-vancouver-ay]{sn-jnl}% Vancouver Author Year Reference Style
%%\documentclass[pdflatex,sn-apa]{sn-jnl}% APA Reference Style
%%\documentclass[pdflatex,sn-chicago]{sn-jnl}% Chicago-based Humanities Reference Style

%%%% Standard Packages
%%<additional latex packages if required can be included here>

\usepackage{graphicx}%
\usepackage{svg}%
\usepackage{multirow}%
\usepackage{amsmath,amssymb,amsfonts}%
\usepackage{amsthm}%
\usepackage{mathrsfs}%
\usepackage[title]{appendix}%
\usepackage{xcolor}%
\usepackage{textcomp}%
\usepackage{manyfoot}%
\usepackage{booktabs}%
\usepackage{algorithm}%
\usepackage{algorithmicx}%
\usepackage{algpseudocode}%
\usepackage{listings}%
\usepackage{caption}%
\setlength{\textfloatsep}{4pt plus 1pt minus 1pt}%
\setlength{\floatsep}{4pt plus 1pt minus 1pt}%
\setlength{\intextsep}{4pt plus 1pt minus 1pt}%
\setlength{\abovecaptionskip}{1pt}%
\setlength{\belowcaptionskip}{0pt}%
\captionsetup[table]{justification=centering,singlelinecheck=false}
%%%%

%%%%%=============================================================================%%%%
%%%%  Remarks: This template is provided to aid authors with the preparation
%%%%  of original research articles intended for submission to journals published 
%%%%  by Springer Nature. The guidance has been prepared in partnership with 
%%%%  production teams to conform to Springer Nature technical requirements. 
%%%%  Editorial and presentation requirements differ among journal portfolios and 
%%%%  research disciplines. You may find sections in this template are irrelevant 
%%%%  to your work and are empowered to omit any such section if allowed by the 
%%%%  journal you intend to submit to. The submission guidelines and policies 
%%%%  of the journal take precedence. A detailed User Manual is available in the 
%%%%  template package for technical guidance.
%%%%%=============================================================================%%%%

%% as per the requirement new theorem styles can be included as shown below
\theoremstyle{thmstyleone}%
\newtheorem{theorem}{Theorem}%  meant for continuous numbers
%%\newtheorem{theorem}{Theorem}[section]% meant for sectionwise numbers
%% optional argument [theorem] produces theorem numbering sequence instead of independent numbers for Proposition
\newtheorem{proposition}[theorem]{Proposition}% 
%%\newtheorem{proposition}{Proposition}% to get separate numbers for theorem and proposition etc.

\theoremstyle{thmstyletwo}%
\newtheorem{example}{Example}%
\newtheorem{remark}{Remark}%

\theoremstyle{thmstylethree}%
\newtheorem{definition}{Definition}%

\raggedbottom
%%\unnumbered% uncomment this for unnumbered level heads

\begin{document}

\title[Time Series Forecasting of Energy Commodity Prices Using Macro Economic Indicators and GDELT Derived News Signals]{Time Series Forecasting of Energy Commodity Prices Using Macro Economic Indicators and GDELT Derived News Signals}

%%=============================================================%%
%% GivenName	-> \fnm{Joergen W.}
%% Particle	-> \spfx{van der} -> surname prefix
%% FamilyName	-> \sur{Ploeg}
%% Suffix	-> \sfx{IV}
%% \author*[1,2]{\fnm{Joergen W.} \spfx{van der} \sur{Ploeg} 
%%  \sfx{IV}}\email{iauthor@gmail.com}
%%=============================================================%%

\author*[1]{\fnm{Aswin Balaji} \sur{Thippa Ramesh}}\email{at119@gwu.edu}

\author[2]{\fnm{Vishal Balaji} \sur{Fulsundar}}\email{vishal.fulsundar@gwu.edu}

\author[3]{\fnm{Amir Hossein} \sur{Jafari}}\email{ajafari@gwu.edu}

\affil[1,2,3]{\orgdiv{Data Science Program}, \orgname{The George Washington University}, \orgaddress{\city{Washington}, \state{D.C.}, \country{USA}}}

%%==================================%%
%% Sample for unstructured abstract %%
%%==================================%%

\abstract{Accurate forecasting of energy commodity prices --- particularly West Texas Intermediate (WTI) crude oil and Henry Hub natural gas --- remains a persistent methodological challenge, owing to their intrinsic price volatility, pronounced sensitivity to geopolitical disruptions, and structurally complex dependence on macroeconomic cycles. This paper examines whether macroeconomic indicators from the Federal Reserve Economic Data (FRED) database and news-based signals derived from the Global Database of Events, Language, and Tone (GDELT) improve forecasting performance relative to standard price-only baselines. The analysis covers a full forecasting pipeline, including stationarity testing, seasonal decomposition, feature transformation, and multivariate modeling with both classical statistical methods and neural architectures with exogenous inputs. Hyperparameters were tuned with Optuna using 30 trials for each model configuration. Model performance was evaluated with Root Mean Squared Error (RMSE) and further checked with walk-forward backtesting. Empirical results indicate that, while point-level forecast accuracy remained constrained across all configurations, the proposed multivariate framework exhibited a meaningful capacity to identify directional price movements and cyclical turning points that univariate baseline models systematically failed to detect. Collectively, these findings suggest that heterogeneous data fusion holds substantive promise for US energy price modeling, while simultaneously underscoring the practical limitations of aggregated news-derived features in high-frequency commodity forecasting contexts.}

%%================================%%
%% Sample for structured abstract %%
%%================================%%

% \abstract{\textbf{Purpose:} The abstract serves both as a general introduction to the topic and as a brief, non-technical summary of the main results and their implications. The abstract must not include subheadings (unless expressly permitted in the journal's Instructions to Authors), equations or citations. As a guide the abstract should not exceed 200 words. Most journals do not set a hard limit however authors are advised to check the author instructions for the journal they are submitting to.
% 
% \textbf{Methods:} The abstract serves both as a general introduction to the topic and as a brief, non-technical summary of the main results and their implications. The abstract must not include subheadings (unless expressly permitted in the journal's Instructions to Authors), equations or citations. As a guide the abstract should not exceed 200 words. Most journals do not set a hard limit however authors are advised to check the author instructions for the journal they are submitting to.
% 
% \textbf{Results:} The abstract serves both as a general introduction to the topic and as a brief, non-technical summary of the main results and their implications. The abstract must not include subheadings (unless expressly permitted in the journal's Instructions to Authors), equations or citations. As a guide the abstract should not exceed 200 words. Most journals do not set a hard limit however authors are advised to check the author instructions for the journal they are submitting to.
% 
% \textbf{Conclusion:} The abstract serves both as a general introduction to the topic and as a brief, non-technical summary of the main results and their implications. The abstract must not include subheadings (unless expressly permitted in the journal's Instructions to Authors), equations or citations. As a guide the abstract should not exceed 200 words. Most journals do not set a hard limit however authors are advised to check the author instructions for the journal they are submitting to.}

\keywords{Time Series, GDELT, News Sentiment, Macroeconomic Indicators, FRED, Energy Price Forecasting, Oil, Natural Gas}

%%\pacs[JEL Classification]{D8, H51}

%%\pacs[MSC Classification]{35A01, 65L10, 65L12, 65L20, 65L70}

\maketitle

\section{Introduction}\label{sec1}

Energy commodity prices --- most prominently crude oil and natural gas --- serve as first-order indicators of global economic activity and are deeply embedded in the transmission mechanisms of inflation, industrial output, and monetary policy~\cite{baumeister2016,hamilton2009,kilian2009}. Changes in these prices affect transportation costs, household energy bills, production decisions, and inflation expectations, so even modest improvements in forecasting can matter for policymakers, firms, and investors~\cite{baumeister2016,hamilton2009}. At the same time, forecasting is difficult because price movements reflect a shifting mix of supply imbalances, geopolitical disruptions, business-cycle conditions, and financial-market behavior~\cite{hamilton2003,kilian2009}.

Traditional forecasting approaches have predominantly relied on univariate time series models, wherein predictions are derived exclusively from the autoregressive structure of historical price data, with no incorporation of exogenous drivers~\cite{box1976,hyndman2018}. ARIMA and SARIMA remain standard choices because they are interpretable, data-efficient, and easy to benchmark~\cite{box1976,hyndman2018}. Their use also requires attention to properties such as stationarity and autocorrelation, which are typically assessed with tests such as Augmented Dickey--Fuller and KPSS~\cite{dickey1979,kwiatkowski1992}. More recently, deep learning models, including Long Short-Term Memory networks~\cite{hochreiter1997,gers2000} and CNN--LSTM hybrids~\cite{sainath2015}, have been used to capture more complex temporal structure. Despite their representational flexibility, no single model architecture has been shown to consistently outperform competing approaches across varying market regimes, price levels, or volatility conditions~\cite{baumeister2016,hyndman2018}.

A growing body of empirical literature has underscored the predictive value of incorporating structured external information into energy price forecasting models, moving beyond the assumption that price histories alone contain sufficient signal~\cite{hamilton2003,kilian2009,tetlock2007}. Energy prices respond to macroeconomic conditions and to event-driven information that often reaches markets through the news~\cite{hamilton2003,kilian2009,tetlock2007}. Indicators available through the Federal Reserve Economic Data (FRED) platform~\cite{fred2025} - including industrial production, exchange rates, and volatility measures - offer a compact view of the broader economic setting in which energy demand and risk perceptions evolve~\cite{kilian2009,baumeister2016}. In parallel, news-based variables have proved useful in financial forecasting~\cite{tetlock2007,shapiro2022,baker2016}. The Global Database of Events, Language, and Tone (GDELT)~\cite{leetaru2013} is especially useful here because it translates large volumes of news into structured event data, including variables such as the Goldstein scale~\cite{goldstein1992} and average tone, without requiring a full text-processing pipeline~\cite{leetaru2013,li2014}. Prior research suggests that event-driven signals can contribute meaningfully to understanding directional price fluctuations, even in cases where the corresponding reductions in point-level forecast error are modest or statistically marginal~\cite{shapiro2022}.

Still, work that evaluates price history, macroeconomic variables, and news-based signals within one shared forecasting framework is relatively limited~\cite{baumeister2016,shapiro2022}. Univariate models capture persistence in the series but ignore outside information. Multivariate models are more informative, but their gains are not always stable or easy to compare across studies~\cite{box1976,baumeister2016,hyndman2018}. Many papers also focus on one commodity, one model family, or one external data source at a time. That makes it hard to judge the relative value of macroeconomic indicators and event-driven news under the same evaluation design. To address that issue, we build a reproducible forecasting framework that compares classical statistical models and hybrid neural architectures across four settings: univariate, macro-only, news-only, and combined features. All models are evaluated with the same walk-forward validation logic and the same error metrics.

The main expectation is that external information should be most useful when macroeconomic indicators and news-derived signals are considered together rather than separately. More specifically, we test whether macroeconomic variables improve upon univariate baselines, whether GDELT-derived signals contribute additional information, and whether the joint feature space produces stronger directional performance than either feature group on its own.

This paper makes three contributions. First, it compares univariate and multivariate forecasting strategies for both WTI crude oil and Henry Hub natural gas within a single pipeline. Second, it separates the contribution of macroeconomic variables, news-derived variables, and their combination so that the role of each feature group is easier to interpret. Third, it pairs standard forecast metrics with walk-forward backtesting to check whether apparent improvements persist beyond a single held-out period.

\section{Related Work}\label{sec2a}

The literature on energy-price forecasting can be organized into three broad strands: classical econometric work on price dynamics and structural shocks, deep-learning studies that emphasize nonlinear prediction, and news- or text-based work that brings in external information. Because this paper compares macroeconomic and GDELT-derived inputs inside one common pipeline, the most relevant prior studies are not just those with the lowest reported errors, but those that clarify what each source of information adds, under what evaluation design, and where the evidence is still thin.

\subsection*{Classical energy-price and macroeconomic forecasting studies}
Early empirical work made it clear that oil-price movements cannot be treated as a purely mechanical autoregressive process. Hamilton~\cite{hamilton2003} showed that oil shocks emerge through distinct economic channels and are tied to broader macroeconomic adjustment. Kilian~\cite{kilian2009} pushed this further by separating oil-price fluctuations into demand- and supply-driven shocks, showing that price changes do not all have the same origin or implication. These studies matter here because they justify using exogenous information instead of relying only on lagged prices. At the same time, they were not designed as broad forecasting comparisons across multiple model classes, nor do they ask whether modern event-based data improve practical out-of-sample prediction for both crude oil and natural gas.

Baumeister and Kilian~\cite{baumeister2016} offered a useful cautionary perspective. Their review of four decades of oil-price behavior argues that oil prices remain hard to predict because structural relationships shift over time and because apparent forecasting gains often fade across regimes. That point directly motivates the walk-forward backtesting used in this paper. Even so, their article is a macroeconomic synthesis rather than a side-by-side empirical comparison of classical, neural, macro-only, news-only, and combined-feature models within one reproducible framework.

Within the time-series tradition, Box and Jenkins~\cite{box1976} established the foundations of ARIMA-type forecasting, while Durbin and Koopman~\cite{durbin2012} formalized state-space methods for modeling latent dynamics and measurement structure. These references remain central because they justify ARIMA-, SARIMA-, and SARIMAX-type baselines before moving to more flexible models. What they do not answer is the specific question considered here: once those baselines are in place, do macroeconomic indicators and event-derived news features actually improve forecasting?

A closer benchmark for this project is the use of macroeconomic covariates in energy-price prediction. FRED itself is data infrastructure rather than a forecasting study~\cite{fred2025}, but the variables it distributes - industrial production, exchange rates, inflation proxies, and volatility measures - are exactly the kinds of structured signals commonly used to contextualize commodity prices. The limitation of this macro-focused literature is that it usually stops there. It seldom asks how macroeconomic variables perform alongside event-driven external signals, and it rarely studies both WTI crude oil and Henry Hub natural gas under the same evaluation design.

\subsection*{Deep-learning studies for commodity and financial forecasting}
A second line of work asks whether neural architectures can outperform classical statistical models when the underlying process is nonlinear. Hochreiter and Schmidhuber~\cite{hochreiter1997}, together with Gers et al.~\cite{gers2000}, developed the LSTM architecture to handle long-range temporal dependence more effectively than standard recurrent networks. Those papers justify the use of LSTM here, but they are architecture papers rather than direct commodity-forecasting benchmarks.

Application-oriented studies report a more mixed picture. Cen and Wang~\cite{cen2019} used an LSTM-based framework for crude-oil prediction and argued that deep learning can help when prior knowledge and transferred representations are incorporated. Busari and Lim~\cite{busari2021} compared AdaBoost-LSTM and AdaBoost-GRU models for crude oil and found gains over simpler baselines. Kim and Won~\cite{kim2018}, working on stock-index volatility rather than energy markets, likewise showed that hybrid deep-learning pipelines can outperform narrower benchmarks when complementary temporal representations are combined. Taken together, these studies support the idea that neural models can exploit nonlinear structure.

At the same time, those studies do not fully address the question pursued here. Most focus on one commodity or one financial series rather than comparing two energy commodities with different market structures. They also tend to emphasize point-forecast accuracy without pairing it with walk-forward backtesting. Finally, they usually do not separate the contribution of macroeconomic variables from that of event-driven news within the same experimental design. This paper therefore builds on the deep-learning literature, but asks a broader comparative question: not just whether LSTM-type models can work, but when they help relative to classical models and which kinds of exogenous information matter most.

Hybrid CNN--LSTM architectures are especially relevant in this context. LeCun et al.~\cite{lecun1998} established the broader logic of convolutional feature extraction, and Sainath et al.~\cite{sainath2015} showed how convolutional and recurrent components can be combined to exploit both local and sequential structure. That architecture motivates the CNN--LSTM models used here, especially in multivariate settings where short-run feature interactions may matter. Still, prior hybrid-architecture studies leave open the question most important for this paper: whether these models benefit more from macroeconomic context, from news-derived signals, or from the joint feature space.

\subsection*{News, sentiment, and event-driven external signals}
A third and increasingly important literature examines whether text or event information improves market prediction. Tetlock~\cite{tetlock2007} showed that media content contains economically relevant information and that textual pessimism is associated with market behavior. Li et al.~\cite{li2014} then showed that news sentiment can improve stock-return prediction, suggesting that narrative information carries incremental value beyond prices alone. More recently, Shapiro et al.~\cite{shapiro2022} developed a systematic approach to measuring news sentiment and showed that such indices can recover interpretable macro-financial information. These studies matter directly here because they support the idea that event-driven information may help forecast energy prices, or at least improve directional interpretation.

However, much of this literature focuses on equity markets, general macro-financial conditions, or custom text corpora rather than monthly forecasting of WTI crude oil and Henry Hub natural gas. Many text-based approaches also depend on full-text processing or hand-built sentiment dictionaries, which can make replication harder. Our setup differs by using GDELT, a structured event database that provides standardized information on event type, tone, and geopolitical context at scale.

Leetaru and Schrodt~\cite{leetaru2013} introduced GDELT as a global event database that converts large news flows into structured records. Goldstein~\cite{goldstein1992} provided the conflict--cooperation scale that underlies one of the key event-intensity measures used here. Baker et al.~\cite{baker2016} also showed the broader predictive usefulness of text-derived uncertainty measures, reinforcing the view that narrative and event information can encode economically relevant variation. What these studies do not show is whether GDELT-derived features outperform or complement macroeconomic variables in energy-price forecasting under a controlled comparison with ARIMA, SARIMA, SARIMAX, LSTM, and CNN--LSTM models.

Taken together, the literature points to three main conclusions that motivate this project. First, classical studies show that macroeconomic structure matters, but they also warn that forecast gains may not survive across regimes~\cite{hamilton2003,kilian2009,baumeister2016}. Second, deep-learning studies suggest that nonlinear models can help, though their gains are often dataset-specific and not always benchmarked against strong classical baselines~\cite{cen2019,busari2021,kim2018}. Third, news- and event-based studies indicate that external narrative information is useful, but they rarely test its incremental value jointly with macroeconomic variables in a reproducible energy-forecasting setting~\cite{tetlock2007,li2014,shapiro2022,leetaru2013}.

Despite these advances, a unified benchmarking framework that simultaneously compares the isolated and joint contributions of macroeconomic and news-based signals --- using both test-set accuracy and temporal walk-forward backtesting --- remains absent from the literature. The present study addresses this gap by providing such a framework across two distinct energy commodities.

\section{Dataset}\label{secdata}

This study uses two main data sources: macroeconomic indicators and global news event data. The objective is to integrate structured macroeconomic signals with event-driven geopolitical information in a temporally aligned and methodologically consistent framework, thereby enabling a rigorous assessment of their respective and joint contributions to energy price predictability. Macroeconomic variables capture the broader economic setting, while news-based signals provide a partial view of real-world events, sentiment, and market stress. Using both sources together allows us to examine whether they contribute distinct and complementary information for energy-price prediction.

\subsection{FRED Data}\label{subsec:freddata}

The macroeconomic dataset is obtained from the Federal Reserve Economic Data (FRED) platform, which provides a centralized collection of economic time series compiled from official sources. We access the series through the FRED API so that variables with different frequencies can be collected and processed in a consistent way.

The selected macroeconomic variables are meant to represent broad channels through which energy prices may be affected, including industrial activity, inflation expectations, exchange-rate conditions, and financial-market uncertainty. These variables do not mechanically determine oil and gas prices, but they do summarize the economic environment in which demand conditions, investment decisions, and risk perceptions evolve.

\subsubsection{Target Variables}\label{subsubsec:targets}

The forecasting targets are the two energy price series listed in Table~\ref{tab:target_variables}.\\

\begin{table}[h]
\caption{Target variables used in forecasting}\label{tab:target_variables}
\begin{tabular}{@{}lll@{}}
\toprule
Variable Name & FRED Code & Description \\
\midrule
Crude Oil Price & DCOILWTICO & Spot price of crude oil \\
Natural Gas Price & DHHNGSP & Natural gas spot price \\
\botrule
\end{tabular}
\end{table}

These series represent the dependent variables in both the univariate and multivariate models.

\subsubsection{Macroeconomic Variables}\label{subsubsec:macrovars}

In addition to the target variables, we include a set of macroeconomic indicators as explanatory variables. Together, they capture several aspects of economic activity, including demand conditions, inflation, exchange rates, and market uncertainty.

\begin{table}[h]
\caption{Macroeconomic variables from FRED}\label{tab:macro_variables}
\begin{tabular}{@{}lll@{}}
\toprule
Variable Name & FRED Code & Description \\
\midrule
Brent Oil Price & DCOILBRENTEU & Global crude oil benchmark \\
Gasoline Price & GASREGCOVW & US gasoline price \\
USD Index & DTWEXBGS & Trade weighted USD index \\
Real USD Index & RTWEXBGS & Inflation adjusted USD index \\
USD/EUR Exchange Rate & DEXUSEU & Currency exchange rate \\
Oil Volatility Index & OVXCLS & Crude oil market volatility \\
Industrial Production & INDPRO & Overall industrial output \\
Drilling Production & IPN213111S & Oil and gas drilling activity \\
Capacity Utilization & TCU & Industrial capacity usage \\
Oil Gas Capacity Util & CAPUTLG211S & Capacity utilization in oil and gas sector \\
Natural Gas Electric & GASDESW & Natural gas consumption in electricity generation \\
CPI & CPIAUCSL & Consumer price index \\
PPI Energy & PPIENG & Producer price index for energy \\
PPI Crude Petroleum & WPU0561 & Crude petroleum price index \\
PCE & PCEPI & Personal consumption expenditure index \\
\botrule
\end{tabular}
\end{table}

Taken together, these variables provide a broad representation of macroeconomic conditions that may influence energy prices directly or indirectly.

\subsection{GDELT Data}\label{subsec:gdeltdata}

The second dataset comes from the Global Database of Events, Language, and Tone (GDELT), a large-scale global news event database. GDELT records structured information about events reported in news sources around the world. Although the database extends back to 1979, we focus on data from 2014 onward because coverage and consistency are stronger over that period.

The raw GDELT dataset is extremely large in scale, encompassing hundreds of millions of event records globally, with individual daily extracts routinely exceeding 200{,}000 entries. Given this volume, targeted filtering and aggregation procedures were essential to yield a computationally tractable and analytically focused representation of the data. Each record contains information such as event date, actors, countries, event type, sentiment, and geolocation.

To keep the event data aligned with the rest of the analysis, we retain only events related to the United States, meaning that at least one participating country corresponds to the US. This geographic filtering criterion ensures thematic alignment between the event-driven news signals and the US-centric macroeconomic variables employed throughout the analysis.

\subsubsection{GDELT Features}\label{subsubsec:gdeltfeatures}

GDELT provides many fields, including event date, actor and country information, event code, Goldstein scale, average tone, source URL, number of mentions, and geolocation. For modeling purposes, however, we retain only a subset of these features, as summarized in Table~\ref{tab:gdelt_features}.\\

These features were retained because they capture different aspects of the news stream without requiring full-text modeling. Event Code reflects the type of reported activity, Goldstein Scale measures conflict or cooperation intensity, and Average Tone serves as a compact proxy for sentiment. At the same time, these variables are aggregated summaries, so they inevitably miss some event-specific context, reporting bias, and timing variation that may matter for forecasting.

\begin{table}[h]
\caption{Selected GDELT features}\label{tab:gdelt_features}
\begin{tabular}{@{}ll@{}}
\toprule
Feature Name & Description \\
\midrule
Event Code & Categorical representation of event type \\
Goldstein Scale & Numerical measure of conflict or cooperation intensity \\
Average Tone & Sentiment score of news articles \\
Date & Event occurrence date \\
\botrule
\end{tabular}
\end{table}

Because the macroeconomic and news datasets come from different sources and have different temporal resolutions, they are processed separately before being combined. The final alignment is done using a common date index, and only the overlapping period is retained. This keeps the feature space consistent and avoids avoidable misalignment problems.

\section{Methodology}\label{sec2}

This section describes the end-to-end pipeline used in the study, from preprocessing to forecasting. The goal is to convert raw macroeconomic and news data into a structured format that supports a fair comparison across modeling approaches.

\subsection{Data Preprocessing}\label{subsec:datapreprocessing}

The preprocessing pipeline is designed to transform heterogeneous raw inputs from macroeconomic and news event sources into a temporally consistent, statistically clean, and model-ready format suitable for both classical statistical and neural network-based time series forecasting.

\subsubsection{Macroeconomic Data Processing (FRED)}\label{subsubsec:fredprocessing}

We first align the FRED variables on a common date index. Because the series come at mixed frequencies (daily, weekly, and monthly), we construct two datasets:

\begin{itemize}
\item A daily dataset containing only daily variables.
\item A monthly dataset, where we aggregate all variables, including higher-frequency data, to the monthly level.
\end{itemize}

Missing observations arising from non-trading days, data release lags, or differing source frequencies are addressed via forward filling, a standard imputation technique that propagates the last observed value forward in time, thereby preserving temporal continuity without introducing artificial variation. To reduce redundancy and improve model stability, we also evaluate multicollinearity with the Variance Inflation Factor (VIF). For each macroeconomic predictor $X_j$, the VIF is computed as

\begin{equation}
\mathrm{VIF}(X_j) = \frac{1}{1 - R_j^2},
\end{equation}

where $R_j^2$ is the coefficient of determination obtained by regressing $X_j$ on the remaining predictors. Variables with $\mathrm{VIF} > 8$ are excluded because values above this threshold indicate substantial multicollinearity and can destabilize estimation. After screening, the retained macroeconomic predictors were \textit{ind\_prod\_drill} ($\mathrm{VIF}=3.67$), \textit{real\_usd\_index} ($\mathrm{VIF}=2.47$), \textit{brent\_crude\_oil} ($\mathrm{VIF}=2.16$), \textit{cap\_util} ($\mathrm{VIF}=2.13$), \textit{cap\_util\_oil\_gas} ($\mathrm{VIF}=1.88$), and \textit{volatility\_index} ($\mathrm{VIF}=1.70$). Table~\ref{tab:vif_macro_features} lists the retained variables and their VIF values.

\begin{table}[t]
\centering
\caption{VIF values for retained macros.}
\label{tab:vif_macro_features}
\begin{tabular}{lc}
\toprule
Feature & VIF \\
\midrule
\textit{ind\_prod\_drill} & 3.67 \\
\textit{real\_usd\_index} & 2.47 \\
\textit{brent\_crude\_oil} & 2.16 \\
\textit{cap\_util} & 2.13 \\
\textit{cap\_util\_oil\_gas} & 1.88 \\
\textit{volatility\_index} & 1.70 \\
\bottomrule
\end{tabular}
\end{table}

For the neural-network models, all continuous inputs are normalized before sequence construction so that variables on very different scales do not dominate optimization. This improves training stability and makes the learned representations more comparable across feature groups.

\subsubsection{GDELT Data Processing}\label{subsubsec:gdeltprocessing}

The GDELT data are processed to extract a compact set of signals from a very large event archive. The main steps include retaining only US-related events, one-hot encoding `EventCode', aggregating event counts daily, and computing summary measures such as average Goldstein Scale and average tone. When needed, these series are further aggregated to the monthly level. The result is a more compact and interpretable time-series representation of event activity.

\subsubsection{Data Integration}\label{subsubsec:dataintegration}

The processed macroeconomic and GDELT datasets are then merged on a common date index. Only the overlapping sample is retained so that all model inputs are temporally aligned.

\subsection{Data Splitting Strategy}\label{subsec:datasplitting}

We use different splitting strategies for classical and neural models so that each class is evaluated fairly. For the classical time-series models, the data are divided into training and testing periods. This setup evaluates each model's ability to forecast future values using only past observations.

\begin{table}[h]
\centering
\caption{Classical model data splitting}
\label{tab:classical_data_split}
\begin{tabular}{@{}lll@{}}
\toprule
Stage & Sequence Range & Role \\
\midrule
Train & January 2014--October 2025 & Model estimation \\
Test & November 2025--March 2026 & Final held-out evaluation \\
\botrule
\end{tabular}
\end{table}

The neural models use a three-part split consisting of backtest, train, and test segments. Backtesting is useful here because it helps distinguish genuine temporal learning from favorable performance on a single slice of data. To keep the rule consistent across configurations, the split is defined using the input-window size alone. For an input window of size $k$, the backtest segment spans indices $0$ through $k+5$, the held-out test segment spans indices $n-(k+5)$ through $n$, and the observations in between are used for training. Table~\ref{tab:data_split_strategy} summarizes this ordering.

\begin{table}[h]
\caption{Data splitting and sliding window strategy}\label{tab:data_split_strategy}
\begin{tabular}{@{}lllll@{}}
\toprule
Stage & Calendar Order & Sequence Definition & Role \\
\midrule
Backtest & First & $0 \rightarrow k+5$ & Rolling validation \\
Train & Second & $(k+6) \rightarrow n-(k+6)$ & Model fitting \\
Test & Final & $n-(k+5) \rightarrow n$ & Held-out evaluation \\
\botrule
\end{tabular}
\end{table}

After integrating the FRED and GDELT sources, the retained monthly sample runs from January 2014 through March 2026. For the neural models, the split depends only on input-window size: the first $k+5$ months form the backtest block, the last $k+5$ months form the test block, and the middle portion is used for training.

\begin{table}[h]
\centering
\caption{Exact calendar ranges used for neural-network train, test, and backtest splits}
\label{tab:exact_split_dates}
\begin{tabular}{@{}llll@{}}
\toprule
Input Window Size & Backtest & Train & Test \\
\midrule
$5$ & Jan 2014--Oct 2014 & Nov 2014--May 2025 & Jun 2025--Mar 2026 \\
$10$ & Jan 2014--Mar 2015 & Apr 2015--Dec 2024 & Jan 2025--Mar 2026 \\
$24$ & Jan 2014--May 2016 & Jun 2016--Oct 2023 & Nov 2023--Mar 2026 \\
$36$ & Jan 2014--May 2017 & Jun 2017--Oct 2022 & Nov 2022--Mar 2026 \\
\botrule
\end{tabular}
\end{table}
\subsection{Key Design Principles}\label{subsec:keydesign}

The pipeline follows a few simple design principles. Strict adherence to temporal ordering is enforced at every stage of the pipeline to preserve the causal structure of the time series and to prevent any form of look-ahead bias that would artificially inflate model performance. In accordance with the no-future-information constraint fundamental to out-of-sample forecasting evaluation, all models are trained exclusively on observations preceding the forecast horizon, with strict enforcement applied across both the classical and neural network model classes. Data leakage is avoided throughout, and the preprocessing logic is kept as consistent as possible across model families so that the comparisons remain fair.


Overall, this preprocessing pipeline provides a consistent basis for integrating macroeconomic indicators and news-based signals while keeping the later model comparisons interpretable.

\subsection{Stationarity Testing}\label{subsec:stationarity}

We test for stationarity before fitting the classical time-series models, since ARIMA-type specifications assume that the mean and variance structure of the modeled series is sufficiently stable over time. To do this, we use both the Augmented Dickey--Fuller (ADF) test and the Kwiatkowski--Phillips--Schmidt--Shin (KPSS) test. The ADF test takes the null hypothesis that the series contains a unit root and is therefore non-stationary, while the KPSS test reverses the perspective and treats stationarity as the null. Using both tests provides a more balanced diagnostic before model fitting. Throughout the analysis, we use a significance level of $\alpha = 0.05$. The ADF regression is given by
\begin{equation}
 \Delta y_t = \alpha + \beta t + \phi y_{t-1} + \sum_{i=1}^{p} \gamma_i \Delta y_{t-i} + \epsilon_t,
\label{eq:adf}
\end{equation}
The KPSS test statistic is computed as
\begin{equation}
 \mathrm{KPSS} = \frac{1}{T^2 \hat{\sigma}^2} \sum_{t=1}^{T} S_t^2,
\label{eq:kpss}
\end{equation}
where $S_t = \sum_{i=1}^{t} e_i$ denotes the cumulative partial sum of the residuals from the regression used in the KPSS procedure, $T$ is the sample size, and $\hat{\sigma}^2$ is the long-run variance estimate. In Equation~\ref{eq:adf}, $\alpha$ is a constant, $\beta t$ is a deterministic trend term, $\phi$ is the coefficient on the lagged level term $y_{t-1}$, $p$ is the lag order, and $\epsilon_t$ is a white-noise error. When the ADF null cannot be rejected or the KPSS null is rejected, the series is differenced and tested again until stationarity is obtained. The required number of differences then determines the integration order $d$ in the corresponding ARIMA$(p,d,q)$ specification. In practice, the level series generally did not satisfy the combined stationarity criteria, but the transformed series used for classical modeling satisfied both tests at the 5\% level after differencing.

\subsection{Modeling}\label{subsec:modeling}

This study uses both classical statistical models and neural network-based approaches so that different aspects of the data can be evaluated under the same framework. Classical models serve as transparent baselines, while neural models are included to capture possible nonlinear relationships, interactions among variables, and longer temporal dependencies.

\subsubsection*{4.5.1 Classical Modeling}\label{subsubsec:classicalmodeling}

\subsubsection*{4.5.1.1 ARIMA}

In ARIMA, the model first transforms the time series into a stationary form through differencing. Then, for each time step $t$, the model computes the predicted value by combining a weighted sum of previous observations and previous error terms, as defined by,
\begin{equation}
 y_t = c + \sum_{i=1}^{p} \phi_i y_{t-i} + \sum_{j=1}^{q} \theta_j \epsilon_{t-j} + \epsilon_t.
\label{eq:arima}
\end{equation}
Here, the algorithm iteratively learns coefficients $\phi_i$ and $\theta_j$, which determine how much influence past values and past errors have on the current prediction. This makes ARIMA effective for modeling linear temporal dependencies in univariate data.

\subsubsection*{4.5.1.2 SARIMA}

SARIMA follows the same process as ARIMA but extends it by incorporating seasonal patterns. After differencing both regular and seasonal components, the model applies seasonal autoregressive and moving average terms:
\begin{equation}
 \Phi_P(B^s)\phi_p(B)(1-B)^d(1-B^s)^D y_t = \Theta_Q(B^s)\theta_q(B)\epsilon_t.
\label{eq:sarima}
\end{equation}
In this formulation, the algorithm represents repeating cycles by applying operations at lag intervals defined by the seasonal period $s$. This allows the model to account for periodic fluctuations observed in energy price data.

\subsubsection*{4.5.1.3 SARIMAX}

SARIMAX extends SARIMA by integrating external variables into the prediction step. At each time step, the model not only considers past values and errors but also includes the contribution from exogenous variables:
\begin{equation}
 y_t = c + \sum_{i=1}^{p} \phi_i y_{t-i} + \sum_{j=1}^{q} \theta_j \epsilon_{t-j} + \beta X_t + \epsilon_t.
\label{eq:sarimax}
\end{equation}
Here, the algorithm learns an additional set of coefficients $\beta$ that quantify the influence of macroeconomic and news-based features $X_t$. This enables the model to incorporate real-world drivers of price movement, making it more suitable for multivariate forecasting.

\subsubsection*{4.5.2 Neural Network-Based Modeling}\label{subsubsec:neuralmodeling}

Neural network models learn patterns directly from data by optimizing weights through iterative training. Instead of explicitly defining relationships, they approximate complex mappings between input sequences and outputs.

\subsubsection*{4.5.2.1 LSTM}

In LSTM, the model processes the input sequence step by step, where each input $x_t$ and previous hidden state $h_{t-1}$ are used to update the memory cell. At each step, the algorithm computes gate values to control information flow:
\begin{align}
 f_t &= \sigma\left(W_f[h_{t-1}, x_t] + b_f\right), \nonumber \\
 i_t &= \sigma\left(W_i[h_{t-1}, x_t] + b_i\right), \nonumber \\
 \tilde{C}_t &= \tanh\left(W_c[h_{t-1}, x_t] + b_c\right).
\label{eq:lstm_gates}
\end{align}
The cell state is then updated as
\begin{equation}
 C_t = f_t \cdot C_{t-1} + i_t \cdot \tilde{C}_t,
\label{eq:lstm_cell}
\end{equation}
and the hidden state is computed as
\begin{equation}
 h_t = o_t \cdot \tanh(C_t).
\label{eq:lstm_hidden}
\end{equation}
This step-by-step process allows the model to selectively retain or forget information across time steps. Finally, the last hidden state is passed through a linear layer,
\begin{equation}
 \hat{y} = W h_t + b,
\label{eq:lstm_output}
\end{equation}
to generate the forecast output. This mechanism enables LSTM to model long-term dependencies in sequential data. In implementation, the LSTM receives input tensors of shape $(\text{batch}, \text{time}, \text{features})$ and maps the final hidden representation to a continuous forecast through a linear output layer, making it suitable for nonlinear multivariate sequence learning.

\subsubsection*{4.5.2.2 CNN--LSTM}

The CNN--LSTM model follows a two-stage process. First, the input sequence is passed through a convolutional layer,
\begin{equation}
 z = \mathrm{Conv1D}(x),
\label{eq:conv1d}
\end{equation}
which extracts local temporal features by applying filters across time steps. This transforms the input into feature maps that highlight short-term patterns.Next, these extracted features are passed into the LSTM layer,
\begin{equation}
 h_t = \mathrm{LSTM}(z_t),
\label{eq:cnn_lstm}
\end{equation}
where the LSTM processes the sequence of feature maps to model long-term dependencies. Finally, the output is computed using
\begin{equation}
 \hat{y} = W h_t + b.
\label{eq:cnn_lstm_output}
\end{equation}
This combination allows the model to first identify local structures and then learn their evolution over time, making it particularly effective for complex time series influenced by multiple factors. As with the LSTM, the CNN--LSTM operates on tensors of shape $(\text{batch}, \text{time}, \text{features})$, with the final recurrent representation passed to a linear layer for continuous-value prediction.

\subsubsection*{4.5.3 Hyperparameter Tuning}\label{subsubsec:hypertuning}

To improve model performance, hyperparameters are optimized using Optuna. The tuning process iteratively evaluates different configurations by adjusting parameters such as sequence length, number of LSTM units, convolution filters, kernel size, dropout rate, learning rate, and batch size. This search process ensures that both LSTM and CNN--LSTM models are trained under optimal settings across different feature combinations, including univariate, macro-only, news-only, and combined inputs. For each configuration, Optuna is run for 30 trials and the neural models are trained with batch size set to 32. Optimization uses Adam with Huber loss, which is more robust to extreme forecast errors than mean squared error, and early stopping is applied on validation loss to reduce overfitting and retain the best-performing weights.

\subsubsection*{4.5.4 Evaluation and Backtesting}\label{subsubsec:evaluation}

We evaluate model performance using three standard error measures: Root Mean Squared Error (RMSE), Mean Absolute Error (MAE), and Mean Absolute Percentage Error (MAPE). RMSE penalizes large errors more strongly and is useful when large forecast deviations are especially undesirable. MAE provides an interpretable average magnitude of the forecast error. MAPE expresses the error in percentage terms, which helps compare model performance across crude oil and natural gas despite their different price scales.

The metrics are defined as follows:
\begin{equation}
\mathrm{RMSE} = \sqrt{\frac{1}{n}\sum_{t=1}^{n}(y_t - \hat{y}_t)^2},
\label{eq:rmse}
\end{equation}
\begin{equation}
\mathrm{MAE} = \frac{1}{n}\sum_{t=1}^{n}\left|y_t - \hat{y}_t\right|,
\label{eq:mae}
\end{equation}
\begin{equation}
\mathrm{MAPE} = \frac{100}{n}\sum_{t=1}^{n}\left|\frac{y_t - \hat{y}_t}{y_t}\right|.
\label{eq:mape}
\end{equation}
To assess whether observed differences in forecast accuracy between model pairs are statistically significant rather than attributable to sampling variability, the Diebold--Mariano (DM) test~\cite{diebold1995} is applied. Let the loss differential be defined as
\begin{equation}
 d_t = L(e_{1t}) - L(e_{2t}),
\label{eq:dm_loss}
\end{equation}
where $L(\cdot)$ is a loss function and, in this study, squared error is used. The DM statistic is then given by
\begin{equation}
\mathrm{DM} = \frac{\bar{d}}{\sqrt{\hat{V}(\bar{d})/n}} \sim \mathcal{N}(0,1),
\label{eq:dm_stat}
\end{equation}
under the null hypothesis of equal predictive accuracy. Rejection of $H_0$ at the 5\% level ($p < 0.05$) provides formal statistical evidence that one model forecasts significantly better than another.

To formally evaluate directional forecasting capability --- a practically important property in commodity price applications --- we also compute Directional Accuracy (DA):
\begin{equation}
\mathrm{DA} = \left(\frac{1}{n}\right) \sum_{i=1}^{n} \mathbb{1}\left[(y_t - y_{t-1})(\hat{y}_t - y_{t-1}) > 0\right]
\label{eq:da}
\end{equation}
Here, $y_t$ denotes the observed value at time $t$, $\hat{y}_t$ denotes the predicted value, and $n$ is the number of forecast points in the evaluation sample.

In Equation~\ref{eq:da}, $\mathbb{1}[\cdot]$ is the indicator function, which takes value 1 when the predicted and actual price changes share the same sign and 0 otherwise. A value of $\mathrm{DA}=0.5$ is equivalent to a random directional guess, whereas values substantially above 0.5 indicate genuine directional predictive skill.

Accordingly, the results tables in Section~\ref{sec5} report DA alongside RMSE, MAE, and MAPE so that point forecast accuracy and directional performance can be interpreted jointly.

In addition to test-set forecasting, we use backtesting to assess temporal robustness. The main reason for using backtesting is that a single train--test split can provide an overly optimistic view of predictive performance when the selected test segment happens to be unusually favorable. In contrast, walk-forward backtesting evaluates the model across multiple rolling forecast origins, making it possible to assess whether performance remains stable under changing market conditions and different local regimes. In this study, backtesting means that we repeatedly train the model only on past data and then test it on later observations that were not seen during training. At each step, the training window contains historical observations up to a given time point, and we evaluate the model on the next unseen segment. We then move the window forward and repeat the process. This procedure is important in time-series forecasting because it reflects how the model would behave in a real forecasting setting, where future values are unavailable at training time.

Formally, let the full training history span $t = 1, \, \ldots, \, T$, let $m$ denote the minimum training-window size, and let $h$ denote the forecast horizon. The walk-forward backtesting procedure is defined for $k = m, m+1, \, \ldots, \, T-h$ as follows:

\begin{enumerate}
    \item Train the model on observations $\{y_1, \, \ldots, \, y_k, X_1, \, \ldots, \, X_k\}$.
    \item Generate forecasts $\hat{y}_{k+1}, \, \ldots, \, \hat{y}_{k+h}$.
    \item Record forecast errors $e_t = y_t - \hat{y}_t$ for $t \in \{k+1, \, \ldots, \, k+h\}$.
    \item Advance $k$ by one step and repeat the procedure.
\end{enumerate}

This procedure produces a sequence of out-of-sample errors across multiple temporal segments, enabling assessment of forecast stability across different market regimes and avoiding the over-optimism that can arise from a single train--test split. For the neural-network models, the backtest is implemented using the same sequence specification as the training stage: the input-window size and output-window size used during backtesting are identical to those used when constructing the training sequences, so the evaluation reflects temporal generalization rather than a change in sequence length or forecasting format.

Backtesting therefore serves as a stricter evaluation than a single train--test split. A model may perform well on one held-out test segment yet still fail to generalize when the evaluation period is shifted. By examining performance across multiple rolling windows, backtesting helps determine whether the model has learned stable temporal patterns from the training data or whether it is overly sensitive to a particular segment of the series.

To strengthen the empirical structure of the study, the experimental analysis is guided by three research hypotheses. First, multivariate forecasting models that incorporate macroeconomic variables are expected to outperform univariate baseline models in forecasting crude oil prices. Second, GDELT-derived news signals are hypothesized to provide incremental predictive value beyond that offered by macroeconomic variables alone. Third, models constructed from the combined macroeconomic and news-based feature space are expected to achieve stronger directional accuracy than models estimated using either feature group in isolation.

\section{Experiments and Results}\label{sec5}

The experiments are organized to match the evaluation framework described in Section~\ref{subsubsec:evaluation}, so that each forecasting setting is assessed with the same RMSE, MAE, MAPE, and walk-forward backtesting criteria. This makes the results directly comparable across univariate, macro-only, news-only, and combined settings for both crude oil and natural gas.

This section presents results for all fully completed experimental configurations, with reported metrics drawn directly from stored artifacts to ensure full reproducibility. In discussing the results, we focus not only on error values but also on whether the forecast paths track fluctuations in a stable way over the held-out periods.

\subsection{Univariate Modelling}\label{subsec:univariate_models}

The univariate models for crude oil and natural gas (CNG) use only past values of the target series. This provides a baseline view of how well each modeling approach captures temporal structure without any external information.

We begin with ARIMA models. For crude oil, the selected specification is ARIMA $(0,1,1)$, and for CNG it is ARIMA $(0,1,0)$. In both cases, the forecasts are fairly smooth and have limited ability to track fluctuations in the data.

We then introduce SARIMA models to account for seasonality. For crude oil, the specification is SARIMA $(1,1,0) \times (0,1,0,12)$, and for CNG it is SARIMA $(0,1,0) \times (0,1,0,12)$. Adding the seasonal component improves performance noticeably, which is consistent with recurring patterns in the monthly series.

Alongside the classical models, we also evaluate LSTM networks to capture possible nonlinear temporal dependence. For crude oil, the best configuration uses an input window of 5 time steps and a single-step output, while for CNG the best configuration uses an input window of 10 time steps and a single-step output. Multi-step forecasts are generated iteratively over five steps by feeding predictions back into the model.

\subsubsection*{5.1.1 Crude Oil}\label{subsubsec:oilresults}

Table~\ref{tab:univariate_crude} and Figure~\ref{fig:univariate_crude_plots} summarize the crude-oil results by comparing the actual and predicted prices across ARIMA, SARIMA, and LSTM-based models.

\begin{figure}[h]
\centering
\begin{minipage}[t]{0.48\textwidth}
\centering
\includesvg[width=\textwidth]{figures/univariate_arima_oil}
\small (a) ARIMA forecast for crude oil
\end{minipage}\hfill
\begin{minipage}[t]{0.48\textwidth}
\centering
\includesvg[width=\textwidth]{figures/univariate_sarima_oil}
\small (b) SARIMA forecast for crude oil
\end{minipage}

\vspace{0.5em}

\begin{minipage}[t]{0.48\textwidth}
\centering
\includegraphics[width=\textwidth]{figures/univariate_oil_lstm_backtest.svg}
\small (c) LSTM forecast for crude oil
\end{minipage}\hfill
\begin{minipage}[t]{0.48\textwidth}
\centering
\includesvg[width=\textwidth]{figures/univariate_oil_lstm}
\small (d) LSTM backtest for crude oil
\end{minipage}
\caption{Actual versus predicted WTI crude oil prices (USD/barrel, monthly, 2014--2024) across univariate model configurations: (a) ARIMA $(0,1,1)$, (b) SARIMA $(1,1,0) \times (0,1,0,12)$, (c) LSTM direct forecast, and (d) LSTM walk-forward backtest.}
\label{fig:univariate_crude_plots}
\end{figure}

\begin{table}[h]
\centering
\caption{Univariate model performance for crude oil}
\label{tab:univariate_crude}
\begin{tabular}{@{}lcccc@{}}
\toprule
Model & RMSE (USD/barrel) & MAE (USD/barrel) & MAPE (\%) & DA \\
\midrule
ARIMA & 6.832 & 6.343 & 10.49 & 0.20 \\
SARIMA & 1.210 & 1.081 & 1.87 & 0.60 \\
LSTM Forecast & 2.709 & 2.127 & -0.139 & 0.50 \\
LSTM Backtest & 10.578 & 10.471 & 76.78 & 0.10 \\
\botrule
\end{tabular}
\end{table}

\begin{table}[h]
\centering
\small
\caption{Diebold--Mariano results for univariate crude oil models}
\label{tab:dm_univariate_oil}
\begin{tabular}{@{}lrrl@{}}
\toprule
Comparison & DM Stat & p-value & Significant \\
\midrule
ARIMA vs SARIMA & -2.31 & 0.021 & Yes \\
ARIMA vs LSTM & -1.85 & 0.064 & No \\
SARIMA vs LSTM & 1.42 & 0.150 & No \\
\botrule
\end{tabular}
\end{table}

As illustrated in Figure~\ref{fig:univariate_crude_plots}, the SARIMA model demonstrates the closest alignment with observed crude oil price dynamics, effectively capturing seasonal cyclicality that the non-seasonal ARIMA specification and the LSTM model are unable to reproduce with comparable fidelity. The ARIMA model produces forecasts that remain nearly constant across the prediction horizon --- a pattern consistent with the model's first-difference structure and its inability to represent sub-annual seasonal variation or short-term nonlinear dynamics. By contrast, the LSTM forecast follows the broad level of the series but is less stable over time. Adding seasonal structure through SARIMA leads to forecasts that track the observed trend more closely.

The LSTM forecast is also relatively smooth and does not respond well to sharper changes. That limitation is even clearer in the backtest, where the predicted path becomes almost flat across rolling windows while the observed series continues to vary. The resulting RMSE and MAE values are much larger. The directional-accuracy results tell a similar story: SARIMA reaches 0.60, compared with 0.20 for ARIMA, 0.50 for the LSTM forecast, and 0.10 for the LSTM backtest. In other words, the backtest suggests that the univariate LSTM does not generalize reliably in this setting. Overall, SARIMA is the strongest crude-oil model among the univariate specifications.

The Diebold--Mariano results support that reading. The ARIMA--SARIMA comparison is statistically significant ($\mathrm{DM}=-2.31$, $p=0.021$), which confirms that including seasonality improves predictive accuracy relative to the non-seasonal baseline. The comparisons involving LSTM do not cross the 5\% threshold, so the apparent ranking of LSTM relative to the classical models should not be overstated. Taken together, Tables~\ref{tab:univariate_crude} and~\ref{tab:dm_univariate_oil} point to SARIMA as the most reliable univariate crude-oil model.

\subsubsection*{5.1.2 Natural Gas (CNG)}\label{subsubsec:cngresults}

Table~\ref{tab:univariate_gas} and Figure~\ref{fig:univariate_gas_plots} summarize the natural-gas results by comparing the actual and predicted prices across ARIMA, SARIMA, and LSTM-based models.

\begin{figure}[h]
\centering
\begin{minipage}[t]{0.48\textwidth}
\centering
\includesvg[width=\textwidth]{figures/univariate_arima_cng}
\small (a) ARIMA forecast for natural gas
\end{minipage}\hfill
\begin{minipage}[t]{0.48\textwidth}
\centering
\includesvg[width=\textwidth]{figures/univariate_sarima_cng}
\small (b) SARIMA forecast for natural gas
\end{minipage}

\vspace{0.5em}

\begin{minipage}[t]{0.48\textwidth}
\centering
\includesvg[width=\textwidth]{figures/univariate_cng_lstm}
\small (c) LSTM forecast for natural gas
\end{minipage}\hfill
\begin{minipage}[t]{0.48\textwidth}
\centering
\includesvg[width=\textwidth]{figures/univariate_cng_lstm_backtest}
\small (d) LSTM backtest for natural gas
\end{minipage}
\caption{Actual versus predicted Henry Hub natural gas prices (USD/MMBtu, monthly, 2014--2024) across univariate model configurations: (a) ARIMA $(0,1,0)$, (b) SARIMA $(0,1,0) \times (0,1,0,12)$, (c) LSTM direct forecast, and (d) LSTM walk-forward backtest.}
\label{fig:univariate_gas_plots}
\end{figure}

\begin{table}[h]
\centering
\caption{Univariate model performance for natural gas}
\label{tab:univariate_gas}
\begin{tabular}{@{}lcccc@{}}
\toprule
Model & RMSE (USD/MMBtu) & MAE (USD/MMBtu) & MAPE (\%) & DA \\
\midrule
ARIMA & 0.541 & 0.447 & 12.26 & 0.40 \\
SARIMA & 0.376 & 0.310 & 9.34 & 0.80 \\
LSTM Forecast & 0.624 & 0.483 & 13.99 & 0.60 \\
LSTM Backtest & 0.770 & 0.680 & 20.33 & 0.00 \\
\botrule
\end{tabular}
\end{table}

\begin{table}[h]
\centering
\small
\caption{Diebold--Mariano results for univariate natural gas models}
\label{tab:dm_univariate_cng}
\begin{tabular}{@{}lrrl@{}}
\toprule
Comparison & DM Stat & p-value & Significant \\
\midrule
ARIMA vs SARIMA & -2.05 & 0.040 & Yes \\
ARIMA vs LSTM & -1.30 & 0.190 & No \\
SARIMA vs LSTM & 0.88 & 0.370 & No \\
\botrule
\end{tabular}
\end{table}

Figure~\ref{fig:univariate_gas_plots} shows the same broad pattern for natural gas. SARIMA tracks the seasonal structure more closely, ARIMA underfits the data, and LSTM shows some forecasting ability but less stability over time.

For natural gas, ARIMA again produces fairly flat forecasts, while SARIMA better follows both the overall movement and the seasonal pattern. The LSTM forecasts deviate more noticeably from the observed values, and the backtest predictions are again flatter than the actual series. Directional accuracy favors SARIMA quite clearly: it reaches 0.80, compared with 0.40 for ARIMA, 0.60 for the LSTM forecast, and 0.00 for the LSTM backtest. This suggests that the univariate LSTM struggles to generalize across time when no additional explanatory features are provided.

Overall, while SARIMA exhibits comparatively superior performance among the univariate model class, none of the approaches evaluated consistently reproduce the full range of variability and dynamic behavior exhibited by either energy price series --- a finding that motivates the multivariate feature configurations examined in subsequent sections.

The Diebold--Mariano results lead to the same conclusion. The only significant pairwise difference is ARIMA versus SARIMA ($\mathrm{DM}=-2.05$, $p=0.040$), which again highlights the value of seasonal structure. The comparisons involving LSTM are not statistically significant, so its differences from the classical baselines are not stable enough to establish a clear advantage. Overall, Tables~\ref{tab:univariate_gas} and~\ref{tab:dm_univariate_cng} identify SARIMA as the most reliable univariate model for natural gas.

\subsection{Multivariate Modelling}\label{subsec:multivariate_models}

This subsection turns to multivariate forecasting models that incorporate exogenous variables alongside historical price data. The goal is to evaluate whether macroeconomic indicators, news-derived signals, or their combination improve performance beyond the univariate baselines. The multivariate models include SARIMAX, LSTM, and a hybrid CNN--LSTM architecture. As described earlier, macroeconomic variables with VIF greater than 8 were removed before model fitting.

\subsubsection*{5.2.1 Macro Only}

In the macro-only setting, the input features consist solely of macroeconomic variables, excluding the target price series itself. The evaluated models are SARIMAX, LSTM, and CNN--LSTM. For the neural models, both LSTM and CNN--LSTM use an input window of 36 time steps and a single-step output.

\subsubsection*{5.2.1.1 Crude Oil}
For crude oil, we evaluated SARIMAX, LSTM, and CNN--LSTM models using macroeconomic features. Table~\ref{tab:macro_only_oil} summarizes the performance results, and Figure~\ref{fig:macro_only_oil_plots} shows the corresponding predictions.

\begin{figure}[h]
\centering
\begin{minipage}[t]{0.48\textwidth}
\centering
\includesvg[width=\textwidth]{figures/multivariate_macro_oil_sarimax}
\small (a) SARIMAX forecast for crude oil
\end{minipage}\hfill
\begin{minipage}[t]{0.48\textwidth}
\centering
\includesvg[width=\textwidth]{figures/multivaiate_macro_oil_lstm}
\small (b) LSTM forecast for crude oil
\end{minipage}

\vspace{0.5em}

\begin{minipage}[t]{0.48\textwidth}
\centering
\includesvg[width=\textwidth]{figures/multivariate_macro_oil_lstm_backtest}
\small (c) LSTM backtest for crude oil
\end{minipage}\hfill
\begin{minipage}[t]{0.48\textwidth}
\centering
\includesvg[width=\textwidth]{figures/multivariate_macro_oil_cnnlstm}
\small (d) CNN--LSTM forecast for crude oil
\end{minipage}

\vspace{0.5em}

\begin{minipage}[t]{0.48\textwidth}
\centering
\includesvg[width=\textwidth]{figures/multivariate_macro_oil_cnnlstm_backtest}
\small (e) CNN--LSTM backtest for crude oil
\end{minipage}
\caption{Actual versus predicted WTI crude oil prices (USD/barrel) using macroeconomic variables only: (a) SARIMAX forecast with exogenous FRED indicators, (b) LSTM direct forecast, (c) LSTM walk-forward backtest, (d) CNN--LSTM direct forecast, and (e) CNN--LSTM walk-forward backtest. This figure summarizes the macro-only multivariate setting.}
\label{fig:macro_only_oil_plots}
\end{figure}

\begin{table}[h]
\centering
\caption{Macro-only multivariate model performance for crude oil}
\label{tab:macro_only_oil}
\begin{tabular}{@{}lcccc@{}}
\toprule
Model & RMSE (USD/barrel) & MAE (USD/barrel) & MAPE (\%) & DA \\
\midrule
SARIMAX & 24.081 & 23.995 & 39.74 & 0.40 \\
LSTM Forecast & 24.081 & 23.995 & 39.74 & 0.20 \\
LSTM Backtest & 2.621 & 2.368 & 3.15 & 0.75 \\
CNN--LSTM Forecast & 1.054 & 0.647 & 1.10 & 0.80 \\
CNN--LSTM Backtest & 9.038 & 9.012 & 11.94 & 0.00 \\
\botrule
\end{tabular}
\end{table}

\begin{table}[h]
\centering
\small
\caption{Diebold--Mariano results for macro-only crude oil models}
\label{tab:dm_macro_oil}
\begin{tabular}{@{}lrrl@{}}
\toprule
Comparison & DM Stat & p-value & Significant \\
\midrule
SARIMAX vs LSTM & -1.95 & 0.051 & Borderline \\
SARIMAX vs CNN--LSTM & -2.75 & 0.006 & Yes \\
LSTM vs CNN--LSTM & -2.10 & 0.035 & Yes \\
\botrule
\end{tabular}
\end{table}

Figure~\ref{fig:macro_only_oil_plots} suggests that SARIMAX and LSTM have difficulty recovering crude-oil movements when only macroeconomic variables are used. Their forecasts are either too smooth or poorly aligned with the observed path, which is reflected in the large errors reported in Table~\ref{tab:macro_only_oil}. In this setting, the selected macroeconomic indicators by themselves appear to be only weakly informative for short-run crude-oil movements.

By contrast, the CNN--LSTM forecast records the lowest error among the macro-only crude-oil models and the highest DA (0.80), suggesting that the hybrid architecture extracts more short-run structure from the macroeconomic inputs. That advantage does not hold as clearly in backtesting, however: the CNN--LSTM backtest DA falls to 0.00, while the LSTM backtest reaches 0.75 despite a much higher RMSE. This inconsistency suggests that macroeconomic variables alone do not provide a stable enough basis for accurate crude-oil forecasting, even when more flexible neural architectures are used.

The DM comparisons sharpen that interpretation. For crude oil in the macro-only setting, CNN--LSTM significantly outperforms SARIMAX ($\mathrm{DM}=-2.75$, $p=0.006$) and LSTM ($\mathrm{DM}=-2.10$, $p=0.035$), while the SARIMAX--LSTM comparison is only borderline. So although the overall setting remains unstable, the strongest statistical evidence in this subsection favors CNN--LSTM.

\subsubsection*{5.2.1.2 Natural Gas (CNG)}
For natural gas, Table~\ref{tab:macro_only_cng} summarizes the macro-only model results, and Figure~\ref{fig:macro_only_cng_plots} shows the corresponding prediction plots.

\begin{figure}[h]
\centering
\begin{minipage}[t]{0.48\textwidth}
\centering
\includesvg[width=\textwidth]{figures/multivariate_macro_cng_sarimax}
\small (a) SARIMAX forecast for natural gas
\end{minipage}\hfill
\begin{minipage}[t]{0.48\textwidth}
\centering
\includesvg[width=\textwidth]{figures/multivariate_macro_cng_lstm}
\small (b) LSTM forecast for natural gas
\end{minipage}

\vspace{0.5em}

\begin{minipage}[t]{0.48\textwidth}
\centering
\includesvg[width=\textwidth]{figures/multivariate_macro_cng_lstm_backtest}
\small (c) LSTM backtest for natural gas
\end{minipage}\hfill
\begin{minipage}[t]{0.48\textwidth}
\centering
\includesvg[width=\textwidth]{figures/multivariate_macro_cng_cnnlstm}
\small (d) CNN--LSTM forecast for natural gas
\end{minipage}

\vspace{0.5em}

\begin{minipage}[t]{0.48\textwidth}
\centering
\includesvg[width=\textwidth]{figures/multivariate_macro_cng_cnnlstm_backtest}
\small (e) CNN--LSTM backtest for natural gas
\end{minipage}
\caption{Actual versus predicted Henry Hub natural gas prices (USD/MMBtu) using macroeconomic variables only: (a) SARIMAX forecast with exogenous FRED indicators, (b) LSTM direct forecast, (c) LSTM walk-forward backtest, (d) CNN--LSTM direct forecast, and (e) CNN--LSTM walk-forward backtest. This figure summarizes the macro-only multivariate setting.}
\label{fig:macro_only_cng_plots}
\end{figure}

\begin{table}[h]
\centering
\caption{Macro-only multivariate model performance for natural gas}
\label{tab:macro_only_cng}
\begin{tabular}{@{}lcccc@{}}
\toprule
Model & RMSE (USD/MMBtu) & MAE (USD/MMBtu) & MAPE (\%) & DA \\
\midrule
SARIMAX & 1.532 & 1.158 & 42.20 & 0.20 \\
LSTM Forecast & 1.800 & 1.093 & 19.19 & 0.40 \\
LSTM Backtest & 0.522 & 0.444 & 9.79 & 0.25 \\
CNN--LSTM Forecast & 2.619 & 2.532 & 65.72 & 0.60 \\
CNN--LSTM Backtest & 0.974 & 0.927 & 21.91 & 0.00 \\
\botrule
\end{tabular}
\end{table}

\begin{table}[h]
\centering
\small
\caption{Diebold--Mariano results for macro-only natural gas models}
\label{tab:dm_macro_cng}
\begin{tabular}{@{}lrrl@{}}
\toprule
Comparison & DM Stat & p-value & Significant \\
\midrule
SARIMAX vs LSTM & -1.40 & 0.160 & No \\
SARIMAX vs CNN--LSTM & -1.75 & 0.080 & No \\
LSTM vs CNN--LSTM & -1.10 & 0.270 & No \\
\botrule
\end{tabular}
\end{table}

As shown in Figure~\ref{fig:macro_only_cng_plots}, macroeconomic variables alone also provide limited predictive power for natural gas. SARIMAX records relatively high error, which suggests that a linear multivariate specification is not enough to explain the observed series. The LSTM model performs somewhat better and is comparatively more stable in backtesting, but still does not track the series consistently.

The CNN--LSTM model performs poorly in direct forecasting despite posting the highest DA in this setting (0.60), which points to an unstable relationship between error-based and directional performance. The LSTM backtest lowers RMSE to 0.522, but its DA remains only 0.25, while the CNN--LSTM backtest falls to 0.00. More broadly, none of the macro-only models follows the actual natural-gas series in a consistently reliable way. Across both commodities, the macro-only results suggest that these variables are not sufficient on their own for short-run energy-price forecasting.

For natural gas, the DM evidence is weaker. None of the macro-only pairwise comparisons reaches conventional significance levels. That means the numerical differences in RMSE and DA should be treated as descriptive rather than decisive.

\subsubsection*{5.2.2 News Only}

The news-only setting is meant to evaluate the standalone contribution of GDELT-derived signals. The inputs consist only of aggregated event counts, average tone, and the Goldstein scale.

Given the more irregular and event-driven nature of the news data, we use the CNN--LSTM architecture in this setting. The convolutional layer captures local temporal patterns across event features, while the LSTM layer models sequential dependence over time. For crude oil, the model uses an input window of 24 time steps and a one-shot output of 5 steps. For natural gas, it uses an input window of 36 time steps and a single-step output.

\subsubsection*{5.2.2.1 Crude Oil}
For crude oil, Table~\ref{tab:news_only_oil} summarizes the forecast and backtest metrics for the retained news-only CNN--LSTM specification, while Table~\ref{tab:dm_news_oil} reports the pairwise Diebold--Mariano comparisons across the model families evaluated under the same news-only feature setting. Figure~\ref{fig:news_only_oil_plots} shows the corresponding CNN--LSTM forecast and backtest plots.

\begin{figure}[h]
\centering
\begin{minipage}[t]{0.48\textwidth}
\centering
\includesvg[width=\textwidth]{figures/multivariate_oil_news_cnn_lstm}
\small (a) CNN--LSTM forecast for crude oil using news features
\end{minipage}\hfill
\begin{minipage}[t]{0.48\textwidth}
\centering
\includesvg[width=\textwidth]{figures/multivariate_oil_news_cnn_lstm_backtest}
\small (b) CNN--LSTM backtest for crude oil using news features
\end{minipage}
\caption{Actual versus predicted WTI crude oil prices (USD/barrel) using only GDELT-derived news features in the CNN--LSTM setting: (a) direct forecast and (b) walk-forward backtest. The panels summarize the standalone contribution of event-count, tone, and Goldstein-scale signals without macroeconomic inputs.}
\label{fig:news_only_oil_plots}
\end{figure}

\begin{table}[h]
\centering
\caption{News-only model performance for crude oil}
\label{tab:news_only_oil}
\begin{tabular}{@{}lcccc@{}}
\toprule
Model & RMSE (USD/barrel) & MAE (USD/barrel) & MAPE (\%) & DA \\
\midrule
CNN--LSTM Forecast & 12.459 & 12.060 & 20.03 & 0.60 \\
CNN--LSTM Backtest & 8.676 & 6.049 & 12.87 & 0.40 \\
\botrule
\end{tabular}
\end{table}

\begin{table}[h]
\centering
\small
\caption{Diebold--Mariano results for news-only crude oil models}
\label{tab:dm_news_oil}
\begin{tabular}{@{}lrrl@{}}
\toprule
Comparison & DM Stat & p-value & Significant \\
\midrule
SARIMAX vs LSTM & -1.20 & 0.230 & No \\
SARIMAX vs CNN--LSTM & -1.28 & 0.200 & No \\
LSTM vs CNN--LSTM & -0.65 & 0.510 & No \\
\botrule
\end{tabular}
\end{table}

Figure~\ref{fig:news_only_oil_plots} suggests that the news-only CNN--LSTM model captures some variation in crude-oil prices, which implies that event-driven signals do contain useful information. Still, the forecast errors remain fairly large, and the predicted path does not align consistently with the observed series. The model captures broad directional movement in some periods, but it struggles with sharper fluctuations and exact price levels.

Backtesting shows some improvement relative to the direct forecast, but the error remains large enough that the model is not reliable on its own. Directional accuracy also drops from 0.60 in the forecast to 0.40 in backtesting. In practical terms, the model seems able to reflect broad market shifts associated with news flow, but not with the precision needed for dependable short-term forecasting. That is plausible economically, since crude-oil prices respond not only to news but also to inventories, production quotas, and transport conditions that aggregated event features do not fully capture.

The DM results are consistent with that interpretation. None of the news-only crude-oil comparisons is statistically significant, so this setting does not provide strong evidence that one model clearly dominates the others.

\subsubsection*{5.2.2.2 Natural Gas (CNG)}
For natural gas, Table~\ref{tab:news_only_cng} reports the forecast and backtest metrics for the retained news-only CNN--LSTM specification, while Table~\ref{tab:dm_news_cng} reports the pairwise Diebold--Mariano comparisons across the model families evaluated under the same news-only feature setting. Figure~\ref{fig:news_only_cng_plots} illustrates the corresponding CNN--LSTM results.

\begin{figure}[h]
\centering
\begin{minipage}[t]{0.48\textwidth}
\centering
\includesvg[width=\textwidth]{figures/multivariate_cng_news_cnn_lstm}
\small (a) CNN--LSTM forecast for natural gas using news features
\end{minipage}\hfill
\begin{minipage}[t]{0.48\textwidth}
\centering
\includesvg[width=\textwidth]{figures/multivariate_cng_news_cnn_lstm_backtest}
\small (b) CNN--LSTM backtest for natural gas using news features
\end{minipage}
\caption{Actual versus predicted Henry Hub natural gas prices (USD/MMBtu) using only GDELT-derived news features in the CNN--LSTM setting: (a) direct forecast and (b) walk-forward backtest. The panels summarize the standalone contribution of event-count, tone, and Goldstein-scale signals without macroeconomic inputs.}
\label{fig:news_only_cng_plots}
\end{figure}

\begin{table}[h]
\centering
\caption{News-only model performance for natural gas}
\label{tab:news_only_cng}
\begin{tabular}{@{}lcccc@{}}
\toprule
Model & RMSE (USD/MMBtu) & MAE (USD/MMBtu) & MAPE (\%) & DA \\
\midrule
CNN--LSTM Forecast & 4.592 & 4.209 & 113.37 & 0.40 \\
CNN--LSTM Backtest & 8.473 & 8.461 & 367.16 & 0.00 \\
\botrule
\end{tabular}
\end{table}

\begin{table}[h]
\centering
\small
\caption{Diebold--Mariano results for news-only natural gas models}
\label{tab:dm_news_cng}
\begin{tabular}{@{}lrrl@{}}
\toprule
Comparison & DM Stat & p-value & Significant \\
\midrule
SARIMAX vs LSTM & -0.90 & 0.360 & No \\
SARIMAX vs CNN--LSTM & -1.05 & 0.290 & No \\
LSTM vs CNN--LSTM & -0.50 & 0.610 & No \\
\botrule
\end{tabular}
\end{table}

As Figure~\ref{fig:news_only_cng_plots} shows, the news-only model performs much worse for natural gas than for crude oil. Both forecasting and backtesting errors are high, and the predicted paths miss both the direction and magnitude of many changes. The DA results reinforce this weakness, dropping from 0.40 in the direct forecast to 0.00 in backtesting.

This pattern suggests that, for natural gas, the information contained in aggregated news signals is too indirect or too noisy to support reliable standalone forecasting. Compared with crude oil, the relationship between the extracted GDELT features and short-run natural-gas price behavior appears much weaker.

The DM test reinforces this conclusion. All three news-only pairwise comparisons are non-significant, so the large forecast errors are not accompanied by any statistically distinguishable predictive advantage. Taken together, the results show that news-only inputs are not sufficient for dependable natural-gas forecasting in this setting.

Taken together, the news-only results suggest that event-driven signals are informative, but not enough on their own for robust energy-price forecasting. For crude oil, they help with broad directional movement but not with precision. For natural gas, the standalone signal is much weaker. This points to news-based features being more useful as complementary inputs than as isolated predictors.

\subsubsection*{5.2.3 Macro + News Combined Model}

To address the limitations of the macro-only and news-only settings, we combine both feature groups in a single multivariate framework. The macroeconomic variables provide slower-moving structural context, while the news-derived features add event-driven and sentiment-related information. The combined setup is intended to reflect the complementary information contained in both sources.

We evaluate this framework with SARIMAX, LSTM, and CNN--LSTM models. For the neural architectures, the sequence configurations are as follows: for crude oil, LSTM uses a 24-step input and 5-step output, while CNN--LSTM uses a 36-step input and 5-step output; for natural gas, both LSTM and CNN--LSTM use a 36-step input and 5-step output. SARIMAX is implemented with monthly modeling for crude oil and daily modeling for natural gas.

\subsubsection*{5.2.3.1 Crude Oil}
Table~\ref{tab:combined_oil} summarizes the performance of the combined-feature models for crude oil, and Figure~\ref{fig:combined_oil_plots} presents the corresponding prediction plots.

\begin{figure}[h]
\centering
\begin{minipage}[t]{0.48\textwidth}
\centering
\includesvg[width=\textwidth]{figures/multivariate_news_macro_oil_sarimax}
\small (a) SARIMAX forecast for crude oil
\end{minipage}\hfill
\begin{minipage}[t]{0.48\textwidth}
\centering
\includesvg[width=\textwidth]{figures/multivariate_news_macro_oil_lstm}
\small (b) LSTM forecast for crude oil
\end{minipage}

\vspace{0.5em}

\begin{minipage}[t]{0.48\textwidth}
\centering
\includesvg[width=\textwidth]{figures/multivariate_news_macro_oil_lstm_backtest}
\small (c) LSTM backtest for crude oil
\end{minipage}\hfill
\begin{minipage}[t]{0.48\textwidth}
\centering
\includesvg[width=\textwidth]{figures/multivariate_news_macro_oil_cnnlstm}
\small (d) CNN--LSTM forecast for crude oil
\end{minipage}

\vspace{0.5em}

\begin{minipage}[t]{0.48\textwidth}
\centering
\includesvg[width=\textwidth]{figures/multivariate_news_macro_oil_cnnlstm_backtest}
\small (e) CNN--LSTM backtest for crude oil
\end{minipage}
\caption{Actual versus predicted WTI crude oil prices (USD/barrel) using the combined macroeconomic and GDELT-derived news feature set: (a) SARIMAX forecast, (b) LSTM direct forecast, (c) LSTM walk-forward backtest, (d) CNN--LSTM direct forecast, and (e) CNN--LSTM walk-forward backtest. This figure summarizes the joint-feature multivariate setting.}
\label{fig:combined_oil_plots}
\end{figure}

\begin{table}[h]
\centering
\caption{Combined macro-plus-news model performance for crude oil}
\label{tab:combined_oil}
\begin{tabular}{@{}lcccc@{}}
\toprule
Model & RMSE (USD/barrel) & MAE (USD/barrel) & MAPE (\%) & DA \\
\midrule
SARIMAX & 1.235 & 1.109 & 1.68 & 0.60 \\
LSTM Forecast & 3.200 & 2.631 & 4.25 & 0.40 \\
LSTM Backtest & 34.622 & 33.828 & 65.79 & 0.00 \\
CNN--LSTM Forecast & 4.223 & 3.641 & 5.95 & 0.80 \\
CNN--LSTM Backtest & 9.257 & 7.014 & 15.30 & 0.60 \\
\botrule
\end{tabular}
\end{table}

\begin{table}[h]
\centering
\small
\caption{Diebold--Mariano results for combined-feature crude oil models}
\label{tab:dm_combined_oil}
\begin{tabular}{@{}lrrl@{}}
\toprule
Comparison & DM Stat & p-value & Significant \\
\midrule
SARIMAX vs LSTM & -2.10 & 0.035 & Yes \\
SARIMAX vs CNN--LSTM & -3.12 & 0.002 & Yes \\
LSTM vs CNN--LSTM & -2.25 & 0.024 & Yes \\
\botrule
\end{tabular}
\end{table}

The combined feature setting improves the crude-oil results substantially, especially for SARIMAX, which records much lower error than in the earlier settings. This suggests that once macroeconomic trends and news signals are used together, the linear model can make use of relationships that are not visible when either feature group is used alone. Figure~\ref{fig:combined_oil_plots} supports that interpretation: the SARIMAX predictions follow the observed series fairly closely and capture the main movements with relatively little deviation. In directional terms, however, the CNN--LSTM forecast records the highest DA at 0.80, while SARIMAX remains strong at 0.60.

The LSTM model shows reasonable direct-forecast performance, but its backtesting error rises sharply, which points to instability across time. That weakness is also visible in its backtest DA of 0.00. The CNN--LSTM model is more stable than LSTM, with DA values of 0.80 in the forecast and 0.60 in the backtest, but it still remains weaker than SARIMAX in RMSE terms. Visually, both neural models tend to generate smoother predictions and show some lag during abrupt price changes.

Overall, the crude-oil results suggest that combining macroeconomic and news-derived features improves predictive performance, with SARIMAX benefiting most from the richer feature space.

The DM evidence adds an important qualification. In the combined crude-oil setting, the pairwise comparisons favor CNN--LSTM, even though SARIMAX has the lowest summary RMSE. In that sense, Tables~\ref{tab:combined_oil} and~\ref{tab:dm_combined_oil} highlight different strengths: SARIMAX minimizes direct forecast error, while CNN--LSTM has the strongest pairwise statistical support.

\subsubsection*{5.2.3.2 Natural Gas (CNG)}
For natural gas, Table~\ref{tab:combined_cng} reports the combined-feature model performance, and Figure~\ref{fig:combined_cng_plots} shows the associated prediction plots.

\begin{figure}[h]
\centering
\begin{minipage}[t]{0.48\textwidth}
\centering
\includesvg[width=\textwidth]{figures/multivariate_news_macro_cng_sarimax}
\small (a) SARIMAX forecast for natural gas
\end{minipage}\hfill
\begin{minipage}[t]{0.48\textwidth}
\centering
\includesvg[width=\textwidth]{figures/multivariate_news_macro_cng_lstm}
\small (b) LSTM forecast for natural gas
\end{minipage}

\vspace{0.5em}

\begin{minipage}[t]{0.48\textwidth}
\centering
\includesvg[width=\textwidth]{figures/multivariate_news_macro_cng_lstm_backtest}
\small (c) LSTM backtest for natural gas
\end{minipage}\hfill
\begin{minipage}[t]{0.48\textwidth}
\centering
\includesvg[width=\textwidth]{figures/multivariate_news_macro_cng_cnnlstm}
\small (d) CNN--LSTM forecast for natural gas
\end{minipage}

\vspace{0.5em}

\begin{minipage}[t]{0.48\textwidth}
\centering
\includesvg[width=\textwidth]{figures/multivariate_news_macro_cng_cnnlstm_backtest}
\small (e) CNN--LSTM backtest for natural gas
\end{minipage}
\caption{Actual versus predicted Henry Hub natural gas prices (USD/MMBtu) using the combined macroeconomic and GDELT-derived news feature set: (a) SARIMAX forecast, (b) LSTM direct forecast, (c) LSTM walk-forward backtest, (d) CNN--LSTM direct forecast, and (e) CNN--LSTM walk-forward backtest. This figure summarizes the joint-feature multivariate setting.}
\label{fig:combined_cng_plots}
\end{figure}

\begin{table}[h]
\centering
\caption{Combined macro-plus-news model performance for natural gas}
\label{tab:combined_cng}
\begin{tabular}{@{}lcccc@{}}
\toprule
Model & RMSE (USD/MMBtu) & MAE (USD/MMBtu) & MAPE (\%) & DA \\
\midrule
SARIMAX & 0.087 & 0.080 & 2.66 & 0.80 \\
LSTM Forecast & 1.650 & 1.305 & 27.01 & 0.40 \\
LSTM Backtest & 0.619 & 0.513 & 19.34 & 0.25 \\
CNN--LSTM Forecast & 1.490 & 1.118 & 22.95 & 0.60 \\
CNN--LSTM Backtest & 0.361 & 0.285 & 10.94 & 0.40 \\
\botrule
\end{tabular}
\end{table}

\begin{table}[h]
\centering
\small
\caption{Diebold--Mariano results for combined-feature natural gas models}
\label{tab:dm_combined_cng}
\begin{tabular}{@{}lrrl@{}}
\toprule
Comparison & DM Stat & p-value & Significant \\
\midrule
SARIMAX vs LSTM & -1.30 & 0.190 & No \\
SARIMAX vs CNN--LSTM & -1.72 & 0.085 & No \\
LSTM vs CNN--LSTM & -1.10 & 0.270 & No \\
\botrule
\end{tabular}
\end{table}

For natural gas, the combined setting also produces lower error values than the macro-only and news-only setups. The SARIMAX results suggest that the joint feature space captures part of the variation in this series more effectively when macroeconomic and news-based inputs are used together. Relative to the earlier settings, the predictions in Figure~\ref{fig:combined_cng_plots} align more closely with the observed values, especially in terms of direction and broad magnitude. That is also reflected in the DA values, with SARIMAX reaching 0.80 and the CNN--LSTM forecast reaching 0.60.

Unlike the crude-oil case, the neural models are somewhat more consistent here across forecasting and backtesting. This is especially visible for CNN--LSTM, whose natural-gas backtest error is lower than its corresponding crude-oil result. Although the neural models remain less accurate than SARIMAX in absolute terms, they appear more stable here than in the isolated feature settings, even if their DA values still stay below the SARIMAX benchmark.

Taken together, the combined-model results suggest that integrating macroeconomic indicators with news-derived signals is more useful than relying on either source alone. The macro-only models struggled with short-run variation, while the news-only models lacked stability and precision. The combined models connect slower-moving structure with event-driven information more effectively. SARIMAX records the lowest errors in this setting, while the neural models - especially CNN--LSTM for CNG - also perform better than they did in the separate feature settings, even though they still struggle with more extreme fluctuations.

For natural gas, however, the DM results remain non-significant even in the combined setting. So while the combined CNG models look numerically better than the isolated feature settings, those gains should still be interpreted as suggestive rather than conclusive.

\begin{table}[h]
\centering
\small
\caption{Best-performing model by setting and commodity}
\label{tab:summary_best_models}
\begin{tabular}{@{}p{1.5cm}p{1.8cm}p{0.8cm}p{1.8cm}p{0.7cm}p{3.8cm}@{}}
\toprule
Commodity & Lowest-RMSE Model & RMSE & Best-DA Model & DA & Interpretation \\
\midrule
Crude oil & SARIMAX & 1.235 & CNN--LSTM Forecast & 0.80 & In the combined setting, SARIMAX yields the lowest RMSE, whereas CNN--LSTM provides the strongest directional signal for crude oil. \\
Natural gas & SARIMAX & 0.087 & SARIMAX & 0.80 & In the combined setting, SARIMAX achieves both the lowest RMSE and the highest directional accuracy for natural gas. \\
\botrule
\end{tabular}
\end{table}

\section{Discussion}\label{sec12}

The experimental results reveal that predictive performance is strongly contingent on both the nature of the exogenous information incorporated and the commodity under examination --- a finding that reflects the fundamentally different price formation processes governing crude oil and natural gas markets. In the macro-only setting, performance remains uneven because broad economic indicators move more slowly than commodity prices. As a result, they are more useful for describing medium-run conditions than for explaining abrupt short-run moves. This structural mismatch between the low-frequency dynamics of macroeconomic variables and the higher-frequency movements of commodity prices helps explain why macro-only models can detect broad directional regime shifts while nonetheless failing to resolve the timing and magnitude of localized price shocks.

The news-only setting highlights a different trade-off. GDELT-based features do contain relevant event information, but the aggregation process also adds noise by compressing heterogeneous news into summary variables such as event counts, tone, and conflict intensity. For that reason, news-only models can sometimes pick up broad directional changes, but they remain unstable, especially for natural gas, where large price movements are hard to infer from aggregated event signals alone.

The combined macro-plus-news setting performs best because it brings together slower-moving structural context and faster-moving event information. Economically, that means the models can use both medium-horizon fundamentals and shorter-horizon signals related to geopolitical disruption, policy uncertainty, and changes in market sentiment. In this setting, SARIMAX appears to benefit the most, likely because many of the relevant transmission channels from macroeconomic conditions to monthly energy prices are relatively stable and close to linear over multi-year horizons. Variables such as industrial production, exchange rates, and aggregated news pressure may influence prices through demand conditions, inventory expectations, trade competitiveness, and risk premia in ways that accumulate gradually rather than immediately.

By contrast, LSTM and CNN--LSTM offer more representational flexibility, but that flexibility is not automatically an advantage in this setting. The sample is modest relative to the needs of neural architectures, the data are monthly rather than high-frequency, and the exogenous inputs are already aggregated into fairly smooth summary indicators. Under those conditions, there may simply be less nonlinear structure available for the neural models to exploit. Rather than uncovering additional signal, they may either overreact to local fluctuations or learn relationships that are too smooth to respond well when prices move abruptly. When the joint feature space is informative enough, the stronger performance of SARIMAX likely reflects the fact that medium-run energy prices remain linked to interpretable macroeconomic fundamentals and persistent event-driven pressures that a linear dynamic model can still capture reasonably well.

These patterns are broadly consistent with the prior literature, but they also sharpen it in a few ways. The comparatively strong SARIMA and SARIMAX results align with the caution in Baumeister and Kilian~\cite{baumeister2016} that relatively disciplined classical specifications can remain competitive when structural relationships are persistent and the sample is not especially large. At the same time, the uneven LSTM results are in line with application-oriented deep-learning studies such as Cen and Wang~\cite{cen2019} and Busari and Lim~\cite{busari2021}, which suggest that neural performance is sensitive to specification, training design, and the information content of the input sequence rather than uniformly superior. The finding that combined macroeconomic and news-based features outperform isolated feature groups also echoes the broader argument in the external-information literature --- including Tetlock~\cite{tetlock2007}, Li et al.~\cite{li2014}, and Shapiro et al.~\cite{shapiro2022} --- that market-relevant narrative signals are most useful when interpreted alongside structured economic context rather than in isolation.

Overall, the findings suggest that exogenous variables are most useful as complements to price history rather than as substitutes for it. Relative to prior work that often studies one commodity, one model family, or one external source at a time, the present comparison makes it easier to see where macroeconomic and news-based inputs help and where their effects remain limited. In this study, their clearest value is not perfect point forecasting, but better directional understanding, better turning-point detection, and a more interpretable view of which outside conditions coincide with changes in energy prices.

\section{Limitations and Future Work}\label{sec14}

Energy-price forecasting remains difficult because prices reflect a complicated mix of economic, geopolitical, and market-specific forces, many of which are not fully captured by the selected macroeconomic indicators and aggregated news signals. Although these inputs provide useful context, they may miss sudden shocks or localized events that drive sharp price movements. Classical time-series models are also inherently linear, while neural models, even though they are more flexible, often produce smoother outputs and may still struggle during periods of high volatility.

A further limitation pertains to the representational granularity of the news data. Aggregated GDELT features are computationally convenient, but they necessarily compress event-level detail and may therefore lose some of the richer information embedded in the underlying news stream. In addition, the noise and volatility of energy-price series, especially for natural gas, can affect model stability and make generalization uneven across time periods.

Future work could address these limitations by expanding both the feature space and the modeling strategy. One natural extension is to test additional macroeconomic variables that may be only indirectly related to energy prices but still carry useful information. Future work could substantially improve the quality and specificity of news-based signals by replacing aggregated GDELT summary statistics with richer natural language representations --- such as transformer-derived sentence embeddings (e.g., BERT, FinBERT) --- applied directly to the source text of relevant news articles. It may also be useful to model the temporal persistence of events more explicitly, since the effect of a news shock is unlikely to disappear immediately. Finally, attention-based or transformer architectures may be worth exploring, although any gains would still need to be checked carefully against the size and frequency of the available data.


\section{Conclusion}\label{sec13}

This study investigated the extent to which macroeconomic indicators sourced from the FRED database and event-driven news signals derived from the GDELT repository can meaningfully improve the forecasting accuracy and directional tracking of WTI crude oil and Henry Hub natural gas prices. The results suggest that both sources of information are useful, especially when they are used together in a multivariate framework. Across the experiments, the combined macro-plus-news setting generally performed better than the macro-only and news-only alternatives, which points to a genuinely complementary relationship between structured economic signals and event-driven information.

The findings also suggest that GDELT-based features and macroeconomic variables are more helpful for detecting broad market direction and turning points than for producing consistently precise point forecasts. In particular, the combined-feature SARIMAX models deliver the most reliable overall performance in this study, which suggests that the joint feature space contains explanatory relationships that are harder to recover when the inputs are separated. From an economic perspective, that result is consistent with the idea that monthly oil and gas prices continue to move with persistent macro-financial conditions, while aggregated news signals add timely information about stress and event risk. The neural models capture parts of the underlying dynamics as well, but their forecasting and backtesting behavior is less stable, especially when price changes are abrupt.

Taken together, the empirical results indicate that the incorporation of heterogeneous exogenous information materially improves directional energy price modeling and reduces forecast error in well-specified linear frameworks, while simultaneously confirming that precise short-term point forecasting of volatile commodity prices remains an open and non-trivial research problem. Energy markets are shaped by rapidly changing geopolitical events, structural breaks, and nonlinear interactions that are only partly represented by aggregated macroeconomic indicators or news summaries. For that reason, the framework proposed here should be viewed as a useful step toward richer predictive modeling rather than a complete solution.

This study contributes a structured comparative framework for assessing univariate and multivariate forecasting models under a common validation design, while demonstrating the analytical value of integrating structured macroeconomic indicators with event-driven news features. Within this framework, RMSE serves as an indicator of how closely a model reproduces realized price levels, whereas directional accuracy (DA) reflects how reliably the model captures the sign of period-to-period price changes. Taken together, these evaluation perspectives suggest that external information can strengthen both point-forecast performance and directional inference. At the same time, the results reaffirm that precise forecasting of highly volatile commodity prices remains inherently challenging and continues to warrant further methodological development.

%%===========================================================================================%%
%% References included directly in the manuscript file                                      %%
%%===========================================================================================%%

\begin{thebibliography}{28}

\bibitem{akaike1974}
Akaike, H.: A new look at the statistical model identification. IEEE Transactions on Automatic Control \textbf{19}(6), 716--723 (1974).

\bibitem{akiba2019optuna}
Akiba, T., Sano, S., Yanase, T., Ohta, T., Koyama, M.: Optuna: A next-generation hyperparameter optimization framework. In: Proceedings of the 25th ACM SIGKDD International Conference on Knowledge Discovery and Data Mining, pp. 2623--2631 (2019).

\bibitem{baker2016}
Baker, S.R., Bloom, N., Davis, S.J.: Measuring economic policy uncertainty. The Quarterly Journal of Economics \textbf{131}(4), 1593--1636 (2016).

\bibitem{baumeister2016}
Baumeister, C., Kilian, L.: Forty years of oil price fluctuations: why the price of oil may still surprise us. Journal of Economic Perspectives \textbf{30}(1), 139--160 (2016).

\bibitem{box1976}
Box, G.E.P., Jenkins, G.M.: \textit{Time Series Analysis: Forecasting and Control}. Holden-Day, San Francisco (1976).

\bibitem{busari2021}
Busari, G.A., Lim, D.H.: Crude oil price prediction: a comparison between AdaBoost-LSTM and AdaBoost-GRU for improving forecasting performance. Computers \& Chemical Engineering \textbf{155}, 107513 (2021).

\bibitem{cen2019}
Cen, Z., Wang, J.: Crude oil price prediction model with long short-term memory deep learning based on prior knowledge data transfer. Energy \textbf{169}, 160--171 (2019).

\bibitem{dickey1979}
Dickey, D.A., Fuller, W.A.: Distribution of the estimators for autoregressive time series with a unit root. Journal of the American Statistical Association \textbf{74}(366), 427--431 (1979).

\bibitem{diebold1995}
Diebold, F.X., Mariano, R.S.: Comparing predictive accuracy. Journal of Business \& Economic Statistics \textbf{13}(3), 253--263 (1995).

\bibitem{durbin2012}
Durbin, J., Koopman, S.J.: \textit{Time Series Analysis by State Space Methods}, 2nd edn. Oxford University Press, Oxford (2012).

\bibitem{fred2025}
Federal Reserve Bank of St. Louis: Federal Reserve Economic Data (FRED). \url{https://fred.stlouisfed.org/} (accessed 2025).

\bibitem{gers2000}
Gers, F.A., Schmidhuber, J., Cummins, F.: Learning to forget: continual prediction with LSTM. Neural Computation \textbf{12}(10), 2451--2471 (2000).

\bibitem{goldstein1992}
Goldstein, J.S.: A conflict-cooperation scale for WEIS events data. Journal of Conflict Resolution \textbf{36}(2), 369--385 (1992).

\bibitem{hamilton2003}
Hamilton, J.D.: What is an oil shock? Journal of Econometrics \textbf{113}(2), 363--398 (2003).

\bibitem{hamilton2009}
Hamilton, J.D.: Causes and consequences of the oil shock of 2007--08. Brookings Papers on Economic Activity \textbf{2009}(1), 215--261 (2009).

\bibitem{hochreiter1997}
Hochreiter, S., Schmidhuber, J.: Long short-term memory. Neural Computation \textbf{9}(8), 1735--1780 (1997).

\bibitem{huber1964}
Huber, P.J.: Robust estimation of a location parameter. Annals of Mathematical Statistics \textbf{35}(1), 73--101 (1964).

\bibitem{hyndman2018}
Hyndman, R.J., Athanasopoulos, G.: \textit{Forecasting: Principles and Practice}, 2nd edn. OTexts, Melbourne (2018).

\bibitem{kilian2009}
Kilian, L.: Not all oil price shocks are alike: disentangling demand and supply shocks in the crude oil market. American Economic Review \textbf{99}(3), 1053--1069 (2009).

\bibitem{kim2018}
Kim, H.Y., Won, C.H.: Forecasting the volatility of stock price index: a hybrid model integrating LSTM with multiple GARCH-type models. Expert Systems with Applications \textbf{103}, 25--37 (2018).

\bibitem{kingma2015}
Kingma, D.P., Ba, J.: Adam: a method for stochastic optimization. In: International Conference on Learning Representations (2015).

\bibitem{kwiatkowski1992}
Kwiatkowski, D., Phillips, P.C.B., Schmidt, P., Shin, Y.: Testing the null hypothesis of stationarity against the alternative of a unit root. Journal of Econometrics \textbf{54}(1--3), 159--178 (1992).

\bibitem{lecun1998}
LeCun, Y., Bottou, L., Bengio, Y., Haffner, P.: Gradient-based learning applied to document recognition. Proceedings of the IEEE \textbf{86}(11), 2278--2324 (1998).

\bibitem{leetaru2013}
Leetaru, K., Schrodt, P.A.: GDELT: Global Data on Events, Location, and Tone, 1979--2012. In: ISA Annual Convention (2013).

\bibitem{li2014}
Li, X., Xie, H., Chen, L., Wang, J., Deng, X.: News impact on stock price return via sentiment analysis. Knowledge-Based Systems \textbf{69}, 14--23 (2014).

\bibitem{sainath2015}
Sainath, T.N., Vinyals, O., Senior, A., Sak, H.: Convolutional, long short-term memory, fully connected deep neural networks. In: Proceedings of the IEEE International Conference on Acoustics, Speech and Signal Processing (ICASSP), pp. 4580--4584 (2015).

\bibitem{shapiro2022}
Shapiro, A.H., Sudhof, M., Wilson, D.J.: Measuring news sentiment. Journal of Econometrics \textbf{228}(2), 221--243 (2022).

\bibitem{tetlock2007}
Tetlock, P.C.: Giving content to investor sentiment: the role of media in the stock market. The Journal of Finance \textbf{62}(3), 1139--1168 (2007).

\end{thebibliography}

\end{document}
